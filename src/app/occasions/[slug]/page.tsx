import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { getOccasion, getProductsByIds, occasions } from "@/data/mock-catalog";
import { ProductCard, ProductVisual } from "@/components/site-shell";

export function generateStaticParams() { return occasions.map((occasion) => ({ slug: occasion.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const occasion = getOccasion(slug); return { title: occasion?.name ?? "Occasion", description: occasion?.description }; }
export default async function OccasionPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const occasion = getOccasion(slug); if (!occasion) notFound(); const occasionProducts = getProductsByIds(occasion.productIds); return <main><section className="hero category-hero"><div><span className="eyebrow">Shop by occasion / {occasion.eyebrow}</span><h1>{occasion.name} <em>made personal.</em></h1><p className="hero-copy">{occasion.description}</p><div className="hero-actions"><a href="#pieces" className="button button-dark">See the occasion edit <span className="button-icon">↘</span></a></div></div><div className="hero-art"><ProductVisual tone={occasion.accent} large alt={occasion.image.alt} /></div></section><section id="pieces" className="featured-section"><div className="section-intro"><div><span className="eyebrow">A considered edit</span><h2>Pick quickly. Personalise slowly.</h2></div><p>These pieces remain part of their original category too. This page simply gathers the ones that make sense for this moment.</p></div><div className="product-grid">{occasionProducts.map((product) => <ProductCard key={product.id} product={product} />)}</div></section></main>; }
